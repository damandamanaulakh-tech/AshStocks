# Five trigger/control archive audit — 2026-09-24

Read-only review against local AshStocks HEAD `efb8ca934464551f6db3e32ce31cb94f0ea98cd6`. This is source and feature-code evidence, **not an observation of the deployed Render application**. No supplied code was executed; no repository/runtime/raw-asset edits, imports, database access, API requests, orders, Git writes, or deployment were performed.

## Verified coverage and preservation

All five ZIPs match the pinned names, byte lengths and SHA256 values in `data/asset-review/backenddata-release-inventory-2026-09-24.json`. All 45 non-directory members were read and CRC/hash checked: 38 CSV files, five XLSX workbooks and two PNG images. CSV parsing covered 88,298 data records and 1,528,400 data cells plus 423 header cells, with zero CSV row-width errors. All 146 nested workbook XML parts were parsed; 55 worksheets contain 86,105 populated XML cell nodes and zero executable formula cells. Both PNG histograms were visually inspected directly from bounded ZIP bytes; their overlapping distributions are not proof of a tradable prediction. Native Excel calculation/render/pivot/chart behavior was not tested.

`data/asset-review/research-formula-evidence-2026-09-24.json` is the concise machine-readable checkpoint. `data/asset-review/research-formula-source-rows-2026-09-24.json` preserves all 140 explicit formula-definition rows (112 pre-rise/fall, 12 unique-miner, 16 IFR), including every source field and archive/member/CSV-record provenance. The raw archive bytes remain the authoritative complete preservation source. The consolidated JSON includes the independent arithmetic checks; its raw-profile paths identify local exploratory files, not committed files. This paragraph's no-write statements describe the read-only audit, before these findings were published.

## What these packages actually contain

- **Pre-rise/fall v0.2:** 112 textual trigger definitions, eight families/clusters and eight gates. Seventy-nine rows say `UPSTOX_READY`, 33 need external inputs, but all 112 explicitly say `REQUIRES_15Y_BACKTEST`. There is no producer source code despite `CSV_CODE` in the archive name. Output intent is 1–2-week paper watch/entry/avoid/exit/risk context, not a 3–12-month stock-price valuation model.
- **Unique miner v0.3:** 12 textual signals, six named-event tests, and 7,162 daily close-price-derived internal-market records from 1996-01-02 through 2024-07-05. This is not OHLCV; its own readiness table explicitly says open/high/low/volume are missing. The active-stock count ranges from 51 to 1,390; historical membership and adjustment provenance still require verification.
- **Micro v0.6 and pyramid v0.1:** 22,891 parameter occurrences, not 22,891 verified algorithms. All 16 original micro columns match the pyramid projection row-for-row; pyramid adds three mapping columns. There are 5,547 normalized keys, 4,046 duplicate groups, 9,628 blank definitions and 1,513 distinct nonblank definition texts. Fifty-three normalized groups have multiple distinct nonblank texts; those are authority-review candidates, not automatic contradictions or merge permission. Some definition cells contain preservation instructions rather than mathematics. All 30,000 stock slots are `STOCK_n`/`Company n` placeholders and `USER_BATCH_UNVERIFIED`, not fresh NSE securities. All 198 paper-map rows remain `SAMPLE_ONLY_NOT_15Y`, with real trading `NO`; 12 rows lack price.
- **IFR expansion v0.4:** 16 risk/repair definitions and archived output statistics, plus embedded v0.5 hard-check and v0.6 cash-stack workbooks. The v0.5 workbook itself reports trained-best q80/k5 OOS CAGR alpha **−0.3594 percentage points**, original q90/k5 **+0.9135 points**, and repair-state `FAIL/HOLD`. v0.4 headline 842.49x changes to 855.99x in v0.5 because of threshold/rounding source. These are historical research outputs, not current risk permission or expected return.

## Independently reproduced mathematics and important limits

Every published trailing 5/10-record return or mean in nine daily columns reproduces from the 7,162 records with zero mismatches at 1e-9 tolerance. DBI reproduces exactly as `disp / max(abs(ew_ret), 0.0005)`. The full `product(1 + ew_ret)` is 442.4591226682639, matching the rounded IFR baseline headline. This verifies internal arithmetic, not constituent prices, calendar completeness, corporate-action adjustment, costs or investability.

All **72 IFR percentile thresholds** match quantiles calculated from the entire 1996–2024 dataset to six decimals. Recorded miner PX01/PX02/PX05 flags reproduce exactly using **2010–2024 calibration quantiles**, including their use on pre-2010 rows. Thus the historical tables contain retrospective calibration; they cannot be promoted as point-in-time validation without a new training-only chronological replay. The archived v0.5 train/test outputs should remain separate from these full-sample results.

Missing yield-change input is not distinguished from a failed signal: 5,155 records have no `yield_chg_5d` but store `False` for PX05, PX06 and PX12. The 2008 bond-related event checks therefore say `NO_FIRE` despite unavailable bond data. Five-/ten-record rolling warmup blanks exist, but corresponding Boolean signals are also false rather than unavailable.

Many pre-rise definitions remain underspecified (`near`, `threshold`, `small`, swing identification, recent damage). ATR252 and six-month percentile warmup are longer than the descriptive 1–2-week lead window. Support-low comparisons must specify exclusion of the current candle; confirmation-based triggers must not be backdated before confirmation arrives. Zero-range/zero-volume division and unknown external inputs need explicit null contracts. No supplied producer code establishes those choices.

## Current feature-code findings to discuss before correction

1. **Five IDs compute the wrong preserved rule.** `server-parameter-tunnel-patch.mjs:360–369`: `0079` executes the source rule for `0080` (mean extension); `0080` executes `0083` (down-volume ratio); `0081` executes `0084` (ATR expansion); `0084` executes `0087` (strong-open/weak-close count); `0085` executes `0088` (supply-wick cluster). Archive and `data/parameter-catalog-175.json` names/formulas agree; the implementation dispatch differs.
2. **Additional component/warmup drift:** `T12W_0004` at lines 264–267 permits 126 valid ATR observations instead of the source 252 and omits price-above-20D-low. `0009` at line 287 omits the low-bandwidth component. `0012` at line 294 substitutes an absolute width threshold for a percentile. `0018` at line 300 counts unchanged/first-slice volume as up volume because it uses total minus down volume; the source requires strictly up-close volume.
3. **Availability metadata overstates executable coverage:** 64 of the 112 archive IDs appear in the current 175-node catalog, and 49 have explicit evaluator branches. Seven catalog rows labeled `EXECUTABLE/UPSTOX_READY` have no branch and reach `SOURCE_REQUIRED`: 0073, 0078, 0082, 0083, 0086, 0087, 0088. Eight other no-branch rows correctly require external feeds. Forty-eight archive IDs are absent; this is preservation/integration debt, not authorization to activate them.
4. **Historical lift is consumed as a present score constant:** six `VALIDATED_TRIGGER_ROWS` at `server-data-intelligence-patch.mjs:95–101` match the archived pre-fall/combo summaries. `server-intelligence-score-patch.mjs:34–40` averages their lift into regime risk regardless of whether those conditions currently fire; lines 52–75 incorporate that risk into the paper intelligence score. This is not current execution of those archival market-internal formulas.

Call-path distinction is essential: `server.js:257–281` composes the feature patches. Generic `runScanner` attaches the tunnel before sorting (`server-parameter-tunnel-patch.mjs:507–511`), and attachment blends 70% base score with 30% evidence (`466–485`) while preserving hard-gate decisions. The **Upstox** path subsequently attaches institutional evidence (`server-upstox-institutional-patch.mjs:581–582`), restores `score/selection_score` to `base_score` (`507–525`), and sorts on that primary score (`541`). Consequently wrong formula states/advisory evidence persist, and generic scanner scores can be affected, but this audit does **not** claim Upstox primary rank changed. The separate body-driven stock-selection endpoint uses momentum20Percentile primary ranking and has no discovered loader for these archive CSVs.

## Smallest safe next sequence (discussion/approval required)

First preserve this archive/definition/implementation distinction and discuss exact formula-ID, warmup and null contracts. After explicit approval, correct only the agreed implementations and add offline synthetic fixtures that separately fire each named rule, reject incomplete windows, distinguish zero from missing and prove generic-vs-Upstox ranking boundaries. Do not silently invent thresholds, activate the 48 absent rows, overwrite source wording, or delete duplicate occurrences. A later research phase should restore complete point-in-time panels, use training-only thresholds with chronological holdout/label purging, record confirmation availability timestamps, and include costs before promoting any archived results.

**Proof level:** complete byte/table/XML parsing for the assigned five packages; complete reading of the 140 explicit trigger definitions and the control/proof ledgers; selected independent arithmetic reproduction and static current-code call-path review. Not every parameter prose row is mathematically validated; no production observation, native workbook recalculation, complete raw price reconstruction or executable strategy approval is claimed.
